const initialState = {
  accessRight: "ADMIN",
  organizationId: 1,
  operatorId: '',
  stars: 5,
  firstName: '', // active, pending, ended
  lastName: '',
  authToken: '',
}

export function chat(state=initialState, action){
  return state
}